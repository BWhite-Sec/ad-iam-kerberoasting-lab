# Splunk Universal Forwarder — inputs.conf
# Location on forwarder: C:\Program Files\SplunkUniversalForwarder\etc\system\local\inputs.conf
# Host: DC01 (domain controller)
# Sanitized for public repo — no IPs, hostnames beyond the lab's own, or credentials included.

[WinEventLog://Security]
disabled = false
index = ad
